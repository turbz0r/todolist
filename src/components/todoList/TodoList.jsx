import React from 'react';
import TodoListItem from '../todoListItem/TodoListItem';

const TodoList = () => {
    return (
        <ul>
            <TodoListItem />
            <TodoListItem />
            <TodoListItem />
        </ul>
    );
};

export default TodoList;
